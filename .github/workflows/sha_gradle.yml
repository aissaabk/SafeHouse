- name: Generate SHA keys
  run: |
    cd android

    if [ ! -f "settings.gradle" ]; then
      echo "❌ Not a valid Android project"
      exit 1
    fi

    if [ -f "gradlew" ]; then
      chmod +x gradlew
      ./gradlew signingReport
    else
      gradle signingReport
    fi